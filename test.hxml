-lib packhx
-lib vehjo
-cp lib
-cp test
--remap jonas:vehjo
-main Main
-cs bin/cs
-lib instrument
--macro instrument.TimeCalls.hijack("IntDict")
# note: the following call requires `return 0;` to be added to the bottom of
# packhx.IntArray.get_length
# --macro instrument.TimeCalls.hijack("packhx")
# --macro instrument.TraceCalls.hijack("IntDict")
# --macro instrument.TraceCalls.hijack("IntDictTests")
# -D no-inline
# -D benchmark

